<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User | Database HIFI</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="gradient-bg-1">
    <div class="flex h-screen">
        <!-- Sidebar -->
        <aside class="glass-sidebar w-64 hidden md:block">
            <div class="p-6">
                <div class="flex items-center mb-8">
                    <img src="/img/LogoHIFI-2.png" alt="HIFI Admin Logo for edit page with management interface" class="h-12 w-auto">
                    <span class="ml-3 text-white text-lg font-bold">Admin Panel</span>
                </div>
                <nav class="space-y-2">
                    <a href="/dashboard" class="flex items-center px-4 py-3 text-white/80 hover:bg-white/10 rounded-lg">
                        <i class="fas fa-dashboard mr-3"></i>Dashboard
                    </a>
                    <a href="/" class="flex items-center px-4 py-3 text-white/80 hover:bg-white/10 rounded-lg">
                        <i class="fas fa-home mr-3"></i>Home
                    </a>
                    <a href="/auth/logout" class="flex items-center px-4 py-3 text-white/80 hover:bg-white/10 rounded-lg">
                        <i class="fas fa-sign-out-alt mr-3"></i>Logout
                    </a>
                </nav>
            </div>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 overflow-auto">
            <!-- Top Navbar -->
            <nav class="glass-navbar">
                <div class="px-6 py-4 flex justify-between items-center">
                    <h2 class="text-2xl font-bold text-white">Edit User</h2>
                    <span class="text-white/90">
                        <i class="fas fa-user-circle mr-2"></i><%= nama %>
                    </span>
                </div>
            </nav>

            <!-- Edit Form -->
            <div class="p-6">
                <div class="glass-card p-8 max-w-4xl mx-auto">
                    <h3 class="text-2xl font-bold text-white mb-6">
                        <i class="fas fa-user-edit mr-2"></i>Edit Data User: <%= user.username %>
                    </h3>
                    
                    <form action="/dashboard/edit/<%= user._id %>?_method=PUT" method="POST" class="space-y-6">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Username</label>
                                <input type="text" name="username" value="<%= user.username %>" 
                                       class="glass-input" required>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Nama Lengkap</label>
                                <input type="text" name="nama" value="<%= user.nama || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">NPM</label>
                                <input type="number" name="npm" value="<%= user.npm || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Email</label>
                                <input type="email" name="email" value="<%= user.email || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Tempat Lahir</label>
                                <input type="text" name="ttl" value="<%= user.ttl || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Tanggal Lahir</label>
                                <input type="date" name="tgl" value="<%= user.tgl || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Agama</label>
                                <input type="text" name="agama" value="<%= user.agama || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Golongan Darah</label>
                                <input type="text" name="goldar" value="<%= user.goldar || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">No. HP</label>
                                <input type="text" name="hp" value="<%= user.hp || '' %>" 
                                       class="glass-input">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Alamat Rumah</label>
                                <input type="text" name="rumah" value="<%= user.rumah || '' %>" 
                                       class="glass-input">
                            </div>
                            <div class="md:col-span-2">
                                <label class="block text-sm font-medium text-white mb-2">Alamat Kos</label>
                                <input type="text" name="kos" value="<%= user.kos || '' %>" 
                                       class="glass-input">
                            </div>
                        </div>

                        <div class="space-y-4">
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Pendidikan</label>
                                <textarea name="pendidikan" rows="3" class="glass-input"><%= user.pendidikan || '' %></textarea>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Kepanitiaan</label>
                                <textarea name="panitia" rows="3" class="glass-input"><%= user.panitia || '' %></textarea>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Organisasi</label>
                                <textarea name="organisasi" rows="3" class="glass-input"><%= user.organisasi || '' %></textarea>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Pelatihan</label>
                                <textarea name="pelatihan" rows="3" class="glass-input"><%= user.pelatihan || '' %></textarea>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-white mb-2">Prestasi</label>
                                <textarea name="prestasi" rows="3" class="glass-input"><%= user.prestasi || '' %></textarea>
                            </div>
                        </div>

                        <div class="flex space-x-4 mt-8">
                            <button type="submit" class="glass-button flex-1 bg-green-500/20 hover:bg-green-500/30">
                                <i class="fas fa-save mr-2"></i>Simpan Perubahan
                            </button>
                            <a href="/dashboard" class="glass-button flex-1 text-center bg-gray-500/20 hover:bg-gray-500/30">
                                <i class="fas fa-arrow-left mr-2"></i>Kembali
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="/javascript/dom.js"></script>
</body>
</html>
