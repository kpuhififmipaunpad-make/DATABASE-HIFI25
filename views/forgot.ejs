<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lupa Password - HIFI Database</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link rel="icon" type="image/png" href="/img/LogoHIFI-2.png?v=2">
  <link rel="apple-touch-icon" href="/img/LogoHIFI-2.png?v=2">

  <style>
    body {
      display: flex; align-items: center; justify-content: center;
      min-height: 100vh; padding: 20px; position: relative;
    }
    #forgot-background {
      position: fixed; inset: 0; z-index: -1;
      background-size: cover; background-position: center; background-repeat: no-repeat;
    }
    .forgot-container { width: 100%; max-width: 450px; animation: fadeInUp .6s ease-out; }
    .forgot-card {
      background: rgba(255,255,255,.95); backdrop-filter: blur(20px);
      border-radius: 24px; padding: 40px;
      box-shadow: 0 20px 60px rgba(0,0,0,.3);
      border: 1px solid rgba(255,255,255,.3);
    }
    .forgot-illustration { text-align: center; margin-bottom: 32px; }
    .forgot-illustration img { width: 120px; height: auto; animation: float 3s ease-in-out infinite; }
    .forgot-title {
      font-size: 28px; font-weight: 700; text-align: center; margin-bottom: 8px;
      background: linear-gradient(135deg,#DC143C 0%,#1E3A8A 100%);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    }
    .forgot-subtitle { text-align: center; color:#6B7280; margin-bottom: 32px; font-size: 14px; line-height: 1.6; }
    .back-link { display:inline-flex; align-items:center; gap:8px; color:#6B7280; text-decoration:none; margin-bottom:20px; font-size:14px; transition:.3s; }
    .back-link:hover { color:#1E3A8A; transform: translateX(-5px); }
    .form-footer { margin-top: 24px; text-align: center; }
    .form-footer a { color:#1E3A8A; text-decoration:none; font-weight:600; transition:.3s; }
    .form-footer a:hover { color:#DC143C; }

    @media screen and (max-width: 1024px) { #forgot-background { background-image: url('/img/forgot-bg-small.svg'); } }
    @media screen and (min-width: 1026px) { #forgot-background { background-image: url('/img/forgot-bg.svg'); } }
    @media (max-width: 480px) { .forgot-card { padding: 30px 24px; } .forgot-title { font-size: 24px; } }
  </style>
</head>
<body>
  <!-- Background -->
  <div id="forgot-background"></div>

  <!-- Background Shapes -->
  <div class="bg-shape bg-shape-1"></div>
  <div class="bg-shape bg-shape-2"></div>

  <!-- Alert Messages -->
  <% if (message != '') { %>
    <div id="alert-border-3" class="alert-glass alert-<%= status === 'green' ? 'success' : 'error' %>" style="position: fixed; top: 20px; right: 20px; z-index: 10000; max-width: 400px;">
      <i class="fas fa-<%= status === 'green' ? 'check-circle' : 'exclamation-circle' %>"></i>
      <span><%= message %></span>
      <button onclick="dismiss()" style="background: none; border: none; color: inherit; cursor: pointer; margin-left: auto;">
        <i class="fas fa-times"></i>
      </button>
    </div>
  <% } %>

  <div class="forgot-container">
    <div class="forgot-card">
      <a href="/auth/login" class="back-link"><i class="fas fa-arrow-left"></i>Kembali ke Login</a>

      <div class="forgot-illustration">
        <img src="/img/forgot.png" alt="Forgot Password">
      </div>

      <h1 class="forgot-title">Lupa Password?</h1>
      <p class="forgot-subtitle">Masukkan username dan email Anda untuk mereset password</p>

      <!-- Autofill dimatikan, Save password tetap nyala via new-password -->
      <form action="/forgot" method="POST" data-validate autocomplete="off" aria-autocomplete="none" name="forgot_form">
        <div class="form-group">
          <label class="form-label"><i class="fas fa-user"></i> Username</label>
          <div class="input-group-glass">
            <i class="fas fa-user input-icon"></i>
            <input
              type="text"
              name="username"
              class="form-control-glass"
              placeholder="Masukkan username Anda"
              required
              autocomplete="off"
              autocapitalize="none"
              spellcheck="false"
              inputmode="text"
            >
          </div>
        </div>

        <div class="form-group">
          <label class="form-label"><i class="fas fa-envelope"></i> Email</label>
          <div class="input-group-glass">
            <i class="fas fa-envelope input-icon"></i>
            <input
              type="email"
              name="email"
              class="form-control-glass"
              placeholder="nama@example.com"
              required
              autocomplete="off"
              autocapitalize="none"
              spellcheck="false"
              inputmode="email"
            >
          </div>
        </div>

        <div class="form-group">
          <label class="form-label"><i class="fas fa-lock"></i> Password Baru</label>
          <div class="input-group-glass" style="position: relative;">
            <i class="fas fa-lock input-icon"></i>
            <input
              type="password"
              id="password"
              name="password"
              class="form-control-glass"
              placeholder="Minimal 8 karakter"
              minlength="8"
              required
              autocomplete="new-password"
            >
            <button type="button" class="password-toggle" id="seePassButton" aria-label="Tampilkan/Sembunyikan password">
              <i class="fas fa-eye" id="seePassIcon" aria-hidden="true"></i>
            </button>
          </div>
        </div>

        <button type="submit" class="btn-primary-gradient ripple-container" style="width: 100%; margin-top: 24px;">
          <i class="fas fa-key"></i> Reset Password
        </button>
      </form>

      <div class="form-footer">
        <p style="color: #6B7280;">Ingat password Anda? <a href="/auth/login">Login di sini</a></p>
      </div>
    </div>
  </div>

  <script src="/javascript/animations.js"></script>
  <script src="/javascript/dom.js"></script>
  <script>
    function dismiss(){ const el = document.getElementById("alert-border-3"); if (el) el.classList.add("hide"); }
    setTimeout(() => { const el = document.getElementById("alert-border-3"); if (el) dismiss(); }, 5000);
  </script>
</body>
</html>
