<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - HIFI Database</title>

  <!-- Styles -->
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"/>

  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/img/LogoHIFI-2.png?v=2">
  <link rel="apple-touch-icon" href="/img/LogoHIFI-2.png?v=2">
</head>
<body>
  <!-- Background Shapes -->
  <div class="bg-shape bg-shape-1"></div>
  <div class="bg-shape bg-shape-2"></div>

  <!-- Alert Messages -->
  <% if (message != '') { %>
  <div id="alert-border-3" class="alert-glass alert-<%= status === 'green' ? 'success' : 'error' %>">
    <i class="fas fa-<%= status === 'green' ? 'check-circle' : 'exclamation-circle' %>"></i>
    <span><%= message %></span>
    <button onclick="dismiss()">
      <i class="fas fa-times"></i>
    </button>
  </div>
  <% } %>

  <div class="login-container">
    <div class="login-card">
      <a href="/" class="back-link"><i class="fas fa-arrow-left"></i>Kembali ke Home</a>

      <div class="logo-section" style="text-align:center; margin-bottom: 24px;">
        <img src="/img/Logo HIFI - SVG.svg" alt="Logo HIFI" class="animate-float" style="width:100px; height:auto; margin-bottom:16px;" />
        <h1 class="login-title text-gradient" style="text-align:center;">Selamat Datang</h1>
        <p class="login-subtitle" style="text-align:center; color:#6B7280;">Masuk ke akun HIFI Anda</p>
      </div>

      <form action="/auth/login" method="POST" data-validate>
        <div class="form-group">
          <label class="form-label"><i class="fas fa-user"></i> Username (Gunakan NPM anda)</label>
          <div class="input-group-glass">
            <i class="fas fa-user input-icon"></i>
            <input
              type="text"
              name="username"
              class="form-control-glass"
              placeholder="Masukkan username Anda"
              required
              autocomplete="username"
              autofocus
            />
          </div>
        </div>

        <div class="form-group">
          <label class="form-label"><i class="fas fa-lock"></i> Password</label>
          <div class="input-group-glass">
            <i class="fas fa-lock input-icon"></i>
            <input
              type="password"
              id="password"
              name="password"
              class="form-control-glass"
              placeholder="Masukkan password Anda"
              required
              autocomplete="current-password"
            />
            <button
              type="button"
              class="password-toggle"
              id="seePassButton"
              aria-label="Tampilkan/Sembunyikan password"
              aria-pressed="false"
              title="Tampilkan/Sembunyikan password"
            >
              <i class="fas fa-eye" id="seePassIcon" aria-hidden="true"></i>
            </button>
          </div>
        </div>

        <button type="submit" class="btn-primary-gradient ripple-container" style="width:100%; margin-top:24px;">
          <i class="fas fa-sign-in-alt"></i> Masuk
        </button>
      </form>

      <div class="form-footer" style="margin-top:24px; text-align:center;">
        <p style="color:#6B7280; margin-bottom:12px;">
          Apabila Anda mengalami kendala saat masuk,
          silakan menghubungi <strong>ADMD HIFI</strong>.
        </p>
        <p style="color:#6B7280; margin-bottom:12px;"><a href="/forgot">Lupa Password?</a></p>
        <p style="color:#6B7280;">Belum punya akun? <a href="/auth/signup">Daftar Sekarang</a></p>
      </div>
    </div>
  </div>

  <!-- JS -->
  <script src="/javascript/animations.js"></script>
  <!-- Toggle show/hide password dikendalikan penuh oleh dom.js -->
  <script src="/javascript/dom.js"></script>
  <script>
    // Auto-dismiss alert (gunakan dismiss() dari dom.js jika ada, fallback ke sederhana)
    window.dismiss = window.dismiss || function(){
      const el = document.getElementById("alert-border-3");
      if (el) el.classList.add("hide");
    };
    setTimeout(() => {
      const el = document.getElementById("alert-border-3");
      if (el) window.dismiss();
    }, 5000);
  </script>
</body>
</html>
