<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Database HIFI</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="gradient-bg-1">
    <!-- Alert Message -->
    <% if(message && message.length > 0) { %>
    <div id="alert-border-3" class="fixed top-4 right-4 z-50 glass-card p-4 max-w-md" role="alert">
        <div class="flex items-center">
            <i class="fas fa-<%= status === 'green' ? 'check-circle' : 'exclamation-circle' %> text-2xl mr-3 text-white"></i>
            <div class="text-sm font-medium text-white"><%= message %></div>
            <button onclick="dismiss()" class="ml-auto text-white hover:text-gray-200">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
    <% } %>

    <!-- Navbar -->
    <nav class="glass-navbar sticky top-0 z-40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <img src="/img/LogoHIFI-2.png" alt="HIFI Logo with modern design and vibrant colors" class="h-12 w-auto">
                    <span class="ml-3 text-white text-xl font-bold">Database HIFI</span>
                </div>
                <div class="flex items-center space-x-4">
                    <% if(loggedIn) { %>
                        <a href="/profile" class="glass-button text-sm">
                            <i class="fas fa-user mr-2"></i>Profile
                        </a>
                        <% if(user === 'admin') { %>
                        <a href="/dashboard" class="glass-button text-sm">
                            <i class="fas fa-dashboard mr-2"></i>Dashboard
                        </a>
                        <% } %>
                        <a href="/auth/logout" class="glass-button text-sm bg-red-500/20 hover:bg-red-500/30">
                            <i class="fas fa-sign-out-alt mr-2"></i>Logout
                        </a>
                    <% } else { %>
                        <a href="/auth/login" class="glass-button text-sm">
                            <i class="fas fa-sign-in-alt mr-2"></i>Login
                        </a>
                        <a href="/auth/signup" class="glass-button text-sm">
                            <i class="fas fa-user-plus mr-2"></i>Sign Up
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="glass-card p-8 md:p-12 text-center">
            <img src="/img/LogoHIFI-2.png" alt="HIFI Logo main banner with professional branding elements" class="h-32 w-auto mx-auto mb-6">
            <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">
                Selamat Datang di Database HIFI
            </h1>
            <p class="text-xl text-white/90 mb-8">
                Sistem Informasi dan Database Anggota HIFI Unpad
            </p>
            <% if(!loggedIn) { %>
            <div class="flex justify-center space-x-4">
                <a href="/auth/login" class="glass-button text-lg px-8 py-3">
                    <i class="fas fa-sign-in-alt mr-2"></i>Mulai Sekarang
                </a>
            </div>
            <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-12">
                <div class="glass-card p-6">
                    <i class="fas fa-users text-4xl text-white mb-4"></i>
                    <h3 class="text-xl font-bold text-white mb-2">Kelola Data</h3>
                    <p class="text-white/80">Akses dan kelola data anggota HIFI dengan mudah</p>
                </div>
                <div class="glass-card p-6">
                    <i class="fas fa-chart-line text-4xl text-white mb-4"></i>
                    <h3 class="text-xl font-bold text-white mb-2">Statistik</h3>
                    <p class="text-white/80">Lihat statistik dan perkembangan organisasi</p>
                </div>
                <div class="glass-card p-6">
                    <i class="fas fa-shield-alt text-4xl text-white mb-4"></i>
                    <h3 class="text-xl font-bold text-white mb-2">Aman & Terpercaya</h3>
                    <p class="text-white/80">Data Anda dijamin aman dengan enkripsi tingkat tinggi</p>
                </div>
            </div>
            <% } %>
        </div>
    </div>

    <!-- Footer -->
    <footer class="glass-navbar mt-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
            <p class="text-center text-white/80">
                <i class="fas fa-copyright mr-1"></i>2024 HIFI Unpad. All rights reserved.
            </p>
        </div>
    </footer>

    <script src="/javascript/dom.js"></script>
</body>
</html>
