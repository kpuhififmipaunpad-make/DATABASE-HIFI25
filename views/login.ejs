<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Database HIFI</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="gradient-bg-1">
    <!-- Alert Message -->
    <% if(message && message.length > 0) { %>
    <div id="alert-border-3" class="fixed top-4 right-4 z-50 glass-card p-4 max-w-md" role="alert">
        <div class="flex items-center">
            <i class="fas fa-<%= status === 'green' ? 'check-circle' : 'exclamation-circle' %> text-2xl mr-3 text-white"></i>
            <div class="text-sm font-medium text-white"><%= message %></div>
            <button onclick="dismiss()" class="ml-auto text-white hover:text-gray-200">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
    <% } %>

    <div class="min-h-screen flex items-center justify-center px-4 py-12">
        <div class="glass-card p-8 md:p-12 w-full max-w-md">
            <!-- Logo -->
            <div class="text-center mb-8">
                <img src="/img/LogoHIFI-2.png" alt="HIFI Logo for login page with clean modern design" class="h-24 w-auto mx-auto mb-4">
                <h1 class="text-3xl font-bold text-white mb-2">Login</h1>
                <p class="text-white/80">Masuk ke akun Database HIFI Anda</p>
            </div>

            <!-- Login Form -->
            <form action="/auth/login" method="POST" class="space-y-6">
                <div>
                    <label for="username" class="block text-sm font-medium text-white mb-2">
                        <i class="fas fa-user mr-2"></i>Username
                    </label>
                    <input type="text" id="username" name="username" required 
                           class="glass-input" placeholder="Masukkan username">
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-white mb-2">
                        <i class="fas fa-lock mr-2"></i>Password
                    </label>
                    <div class="relative">
                        <input type="password" id="password" name="password" required 
                               class="glass-input pr-12" placeholder="Masukkan password">
                        <button type="button" id="seePassButton" 
                                class="absolute right-3 top-1/2 transform -translate-y-1/2 text-white/70 hover:text-white">
                            <i class="fas fa-eye" id="seePassIcon"></i>
                        </button>
                    </div>
                </div>

                <div class="flex items-center justify-between">
                    <div class="flex items-center">
                        <input type="checkbox" id="remember" name="remember" 
                               class="h-4 w-4 rounded border-white/30 bg-white/20">
                        <label for="remember" class="ml-2 text-sm text-white/90">Ingat saya</label>
                    </div>
                    <a href="/forgot" class="text-sm text-white/90 hover:text-white">Lupa password?</a>
                </div>

                <button type="submit" class="glass-button w-full">
                    <i class="fas fa-sign-in-alt mr-2"></i>Login
                </button>

                <p class="text-center text-white/80 text-sm">
                    Belum punya akun? 
                    <a href="/auth/signup" class="text-white font-semibold hover:underline">Daftar di sini</a>
                </p>
            </form>
        </div>
    </div>

    <script src="/javascript/dom.js"></script>
</body>
</html>
