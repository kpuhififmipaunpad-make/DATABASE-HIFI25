<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Table - Responsive & Searchable</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
            min-height: 100vh;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 30px;
            color: white;
        }

        .header h1 {
            font-size: 28px;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .search-box {
            position: relative;
            max-width: 500px;
        }

        .search-box input {
            width: 100%;
            padding: 12px 40px 12px 15px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            outline: none;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .search-box::after {
            content: "🔍";
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 18px;
        }

        .table-info {
            padding: 20px 30px;
            background: #f8f9fa;
            border-bottom: 2px solid #e9ecef;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
        }

        .table-info span {
            font-size: 14px;
            color: #495057;
            font-weight: 500;
        }

        .last-update {
            color: #667eea;
            font-weight: 600;
        }

        .table-wrapper {
            overflow-x: auto;
            padding: 30px;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            table-layout: fixed;
        }

        thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: sticky;
            top: 0;
            z-index: 10;
        }

        thead th {
            padding: 15px 12px;
            color: white;
            font-weight: 600;
            text-align: left;
            font-size: 14px;
            white-space: nowrap;
            border-bottom: 3px solid #764ba2;
        }

        tbody tr {
            border-bottom: 1px solid #e9ecef;
            transition: all 0.3s ease;
        }

        tbody tr:hover {
            background: #f8f9ff;
            transform: scale(1.01);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.1);
        }

        tbody td {
            padding: 15px 12px;
            color: #495057;
            font-size: 14px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .status-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .status-active {
            background: #d4edda;
            color: #155724;
        }

        .status-inactive {
            background: #f8d7da;
            color: #721c24;
        }

        .no-data {
            text-align: center;
            padding: 60px 20px;
            color: #6c757d;
            font-size: 16px;
        }

        .new-badge {
            background: #ff6b6b;
            color: white;
            padding: 3px 8px;
            border-radius: 12px;
            font-size: 10px;
            margin-left: 8px;
            font-weight: 700;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        /* Responsive Design */
        @media screen and (max-width: 1024px) {
            .header h1 {
                font-size: 24px;
            }
            
            thead th, tbody td {
                padding: 12px 8px;
                font-size: 13px;
            }
        }

        @media screen and (max-width: 768px) {
            body {
                padding: 10px;
            }

            .header {
                padding: 20px;
            }

            .header h1 {
                font-size: 20px;
                margin-bottom: 15px;
            }

            .table-info {
                padding: 15px 20px;
                flex-direction: column;
                align-items: flex-start;
            }

            .table-wrapper {
                padding: 15px;
            }

            table {
                table-layout: auto;
                min-width: 800px;
            }

            thead th, tbody td {
                padding: 10px 6px;
                font-size: 12px;
            }

            .status-badge {
                padding: 4px 8px;
                font-size: 10px;
            }
        }

        @media screen and (max-width: 480px) {
            .header h1 {
                font-size: 18px;
            }

            .search-box input {
                font-size: 14px;
                padding: 10px 35px 10px 12px;
            }

            table {
                min-width: 700px;
            }

            thead th, tbody td {
                padding: 8px 4px;
                font-size: 11px;
            }
        }

        .loading {
            text-align: center;
            padding: 40px;
            color: #667eea;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📊 Data Management System</h1>
            <div class="search-box">
                <input 
                    type="text" 
                    id="searchInput" 
                    placeholder="Cari berdasarkan nama, kode, atau status..."
                    autocomplete="off"
                >
            </div>
        </div>

        <div class="table-info">
            <span>Total Data: <strong id="totalData">0</strong></span>
            <span class="last-update">Update Terakhir: <strong id="lastUpdate">-</strong></span>
        </div>

        <div class="table-wrapper">
            <table id="dataTable">
                <thead>
                    <tr>
                        <th style="width: 5%;">No</th>
                        <th style="width: 10%;">Kode</th>
                        <th style="width: 25%;">Nama</th>
                        <th style="width: 15%;">Kategori</th>
                        <th style="width: 12%;">Status</th>
                        <th style="width: 18%;">Tanggal Update</th>
                        <th style="width: 15%;">Keterangan</th>
                    </tr>
                </thead>
                <tbody id="tableBody">
                    <tr>
                        <td colspan="7" class="loading">⏳ Memuat data...</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Script akan di-include dari script.ejs -->
    <script src="/script.js"></script>
</body>
</html>
